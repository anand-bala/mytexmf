
\RequirePackage[
  includeheadfoot,
  left=1in,
  right=1in,
  top=.5in,
  bottom=.5in
]{geometry}

\RequirePackage[T1]{fontenc} % For international characters
\RequirePackage[utf8]{inputenc} % UTF-8 Input Format
% \RequirePackage{XCharter} % XCharter as the main font
\RequirePackage{fourier}

\RequirePackage[style=mla]{biblatex}

\RequirePackage[english]{babel} % Use english by default
\RequirePackage{csquotes}
\RequirePackage{setspace}

\RequirePackage[colorlinks]{hyperref}

\RequirePackage{amsmath}
\RequirePackage{amsthm}
\RequirePackage{amsfonts}
\RequirePackage{tikz}

\RequirePackage{listings}
\RequirePackage[plain]{algorithm}
\RequirePackage{algpseudocode}

\usetikzlibrary{automata,positioning}



%----------------------------------------------------------------------------------------
%	STRUCTURE MODIFICATIONS
%----------------------------------------------------------------------------------------

\setlength{\parskip}{5pt} % Slightly increase spacing between paragraphs
\onehalfspacing%

\RequirePackage[inline]{enumitem}
\setlist[itemize]{noitemsep, topsep=0pt}
\setlist[enumerate]{noitemsep, topsep=0pt}

% Uncomment to center section titles
%\usepackage{sectsty}
%\sectionfont{\centering}

% Uncomment for Roman numerals for section numbers
%\renewcommand\thesection{\Roman{section}}

%----------------------------------------------------------------------------------------
% FORMAL HELPERS
%----------------------------------------------------------------------------------------

\theoremstyle{definition}

\newtheorem{definition}{Definition}[section]
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}{Proposition}[section]
\newtheorem{claim}{Claim}[section]
\newtheorem{assumption}{Assumption}[section]
\newtheorem{challenge}{Challenge}[section]

\theoremstyle{remark}
\newtheorem{remark}{Remark}
\newtheorem{note}{Note}

